# Production Environment Values — Render Deployment Workbook

Complete environment-variable inventory for deploying both `apps/api` and `apps/web` to Render, built from a fresh inspection of the current codebase (`render.yaml`, `apps/api/src/config/env.ts`, `apps/api/.env.example`, `docs/PRODUCTION_SOURCE_OF_TRUTH.md`) after the seed-system fix (`docs/reports/SEED_SYSTEM_FIX_REPORT.md`). Updated in Production Phase 2C to add the `ordervora-web` variables once Vercel was replaced by a second Render service (`docs/reports/RENDER_BLUEPRINT_FINAL.md`). No deployment was performed while producing this report.

**This file is committed to a public GitHub repository (`abukeeth/core`) and will never contain a real secret value** — every secret cell below is a placeholder or format example only, consistent with `render.yaml`'s own rule for every `sync: false` variable ("must not be a literal in this public repo either") and `docs/runbooks/database-setup.md`'s explicit instruction to never commit a real credential. See **"About the generated secret"** at the end of this document for where the real, generated `COMMERCE_ENCRYPTION_KEY` value actually is.

---

## Environment Variable Inventory (in deployment order)

Order matches exactly how Render's Blueprint prompts for them — the same order they're declared in `render.yaml`.

### 1. `NODE_ENV`

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: production` |
| Must be generated? | No |
| Do you already own it? | N/A — not a credential |
| Example format | `production` |
| Action needed | None — Render sets this automatically, not prompted |

### 2. `PORT`

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: "4000"` |
| Must be generated? | No |
| Do you already own it? | N/A |
| Example format | `4000` |
| Action needed | None — automatic, not prompted |

### 3. `DATABASE_URL`

| | |
|---|---|
| Where it comes from | Your Supabase project — Session Pooler connection string (`docs/reports/SUPABASE_DEPLOYMENT_CHECKLIST.md`) |
| Already available? | **Only if you've already created the Supabase project.** No evidence in this repo that a live Supabase project exists yet — this is the first value you need to go obtain if you haven't already. |
| Must be generated? | No — it's issued by Supabase, not generated by you |
| Do you already own it? | Unknown — confirm you've created the Supabase project and copied the Session Pooler URI before deploying |
| Example format | `postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres?sslmode=require` |
| Action needed | Paste the completed Session Pooler string (password substituted, `sslmode=require` present) when Render prompts |

### 4. `FRONTEND_URL`

| | |
|---|---|
| Where it comes from | `ordervora-web`'s own Render-assigned URL (not known until that service completes its first deploy) |
| Already available? | No — doesn't exist until `ordervora-web`'s deploy happens |
| Must be generated? | No — assigned by Render |
| Do you already own it? | No, not yet |
| Example format | `https://ordervora-web.onrender.com` |
| Action needed | Paste a **temporary placeholder** now (e.g. `https://placeholder.example`); return to Render's Environment tab and set the real value after `ordervora-web`'s deploy |

### 5. `JWT_ACCESS_SECRET`

| | |
|---|---|
| Where it comes from | Render itself |
| Already available? | Generated automatically at deploy time |
| Must be generated? | Yes — but **by Render**, not by you |
| Do you already own it? | N/A |
| Example format | *(never shown to you or committed anywhere — Render stores it internally)* |
| Action needed | None — `generateValue: true`, not prompted, do not override |

### 6. `JWT_REFRESH_SECRET`

| | |
|---|---|
| Where it comes from | Render itself |
| Already available? | Generated automatically at deploy time |
| Must be generated? | Yes — by Render |
| Do you already own it? | N/A |
| Example format | *(never shown to you)* |
| Action needed | None — `generateValue: true`, not prompted. (Declared but not currently read by any module — `apps/api/src/config/env.ts` line 194 — kept reserved.) |

### 7. `JWT_ACCESS_TTL`

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: 15m` |
| Must be generated? | No |
| Do you already own it? | N/A |
| Example format | `15m` |
| Action needed | None — automatic |

### 8. `JWT_REFRESH_TTL`

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: 30d` |
| Must be generated? | No |
| Do you already own it? | N/A |
| Example format | `30d` |
| Action needed | None — automatic |

### 9. `COMMERCE_ENCRYPTION_KEY`

| | |
|---|---|
| Where it comes from | Generated locally — **cannot** use Render's `generateValue: true` (produces base64; this app requires exactly 64 hex characters, validated by `apps/api/src/config/env.ts`'s `HEX_32_BYTES` regex at startup) |
| Already available? | **Yes — already generated for you.** See "About the generated secret" below for delivery (not in this file). |
| Must be generated? | Yes — done, via `openssl rand -hex 32` |
| Do you already own it? | Yes, as of this report |
| Example format | 64 lowercase hex characters, e.g. `a1b2c3d4...` (64 chars total — **not** the real value) |
| Action needed | Paste the real generated value (delivered separately, see below) when Render prompts |

### 10. `ADMIN_EMAIL`

| | |
|---|---|
| Where it comes from | You choose it |
| Already available? | You decide now |
| Must be generated? | No — pick any real email you control |
| Do you already own it? | Yes, by definition — it's your choice, not an external credential |
| Example format | `owner@yourdomain.com` |
| Action needed | Decide the value, paste it when Render prompts. This becomes the real platform `Role.ADMIN` account's login (fixed by `docs/reports/SEED_SYSTEM_FIX_REPORT.md` — no longer a demo account) |

### 11. `ADMIN_PASSWORD`

| | |
|---|---|
| Where it comes from | You choose it |
| Already available? | You decide now |
| Must be generated? | No — but must be strong, and must **not** be `.env.example`'s placeholder text (rejected at startup by `apps/api/src/config/env.ts`'s placeholder check) |
| Do you already own it? | Yes, by definition |
| Example format | A strong passphrase, e.g. 16+ characters, mixed case/digits/symbols — **not** shown here since this is your real credential to choose |
| Action needed | Decide the value, paste it when Render prompts |

### 12. `ADMIN_NAME`

| | |
|---|---|
| Where it comes from | You choose it |
| Already available? | You decide now |
| Must be generated? | No |
| Do you already own it? | Yes, by definition |
| Example format | `Platform Admin` (or your real name/title) |
| Action needed | Decide the value, paste it when Render prompts |

### 13. `SMTP_HOST`

| | |
|---|---|
| Where it comes from | Your SMTP provider (e.g. Resend's SMTP relay, or any other SMTP provider) |
| Already available? | Unknown — confirm you have an SMTP provider account |
| Must be generated? | No — issued by the provider |
| Do you already own it? | Unknown — confirm before deploying |
| Example format | `smtp.resend.com` |
| Action needed | Paste your provider's host when Render prompts |

### 14. `SMTP_PORT`

| | |
|---|---|
| Where it comes from | Your SMTP provider |
| Already available? | Unknown |
| Must be generated? | No |
| Do you already own it? | Unknown |
| Example format | `587` |
| Action needed | Paste your provider's port when Render prompts |

### 15. `SMTP_USER`

| | |
|---|---|
| Where it comes from | Your SMTP provider |
| Already available? | Unknown |
| Must be generated? | No |
| Do you already own it? | Unknown |
| Example format | `resend` or an API-key-style username, provider-dependent |
| Action needed | Paste your provider's auth username when Render prompts |

### 16. `SMTP_PASSWORD`

| | |
|---|---|
| Where it comes from | Your SMTP provider |
| Already available? | Unknown |
| Must be generated? | No |
| Do you already own it? | Unknown |
| Example format | Provider-issued API key / password — not shown here |
| Action needed | Paste your provider's auth password/API key when Render prompts |

### 17. `SMTP_FROM_ADDRESS`

| | |
|---|---|
| Where it comes from | You choose it (must be a sender address your SMTP provider allows) |
| Already available? | You decide now |
| Must be generated? | No |
| Do you already own it? | Depends on your provider's verified-sender setup |
| Example format | `orders@yourdomain.com` |
| Action needed | Paste the value when Render prompts |

### 18. `OPENAI_API_KEY`

| | |
|---|---|
| Where it comes from | OpenAI dashboard (platform.openai.com → API keys) |
| Already available? | Unknown — confirm you have an OpenAI account with billing enabled |
| Must be generated? | No — issued by OpenAI |
| Do you already own it? | Unknown |
| Example format | `sk-...` (OpenAI's standard key format) |
| Action needed | Paste your key when Render prompts. (Alternative: `ANTHROPIC_API_KEY` or `GEMINI_API_KEY` work too, per the app's provider-priority order, but neither is declared in `render.yaml` — add manually via Render's Environment tab post-deploy if you prefer one of those instead of OpenAI.) |

### 19. `GOOGLE_MAPS_API_KEY` *(optional)*

| | |
|---|---|
| Where it comes from | Google Cloud Console (Places API) |
| Already available? | Unknown |
| Must be generated? | No |
| Do you already own it? | Unknown |
| Example format | `AIza...` (Google's standard key format) |
| Action needed | Only needed if you want Google Places import live. May be left blank at deploy time. |

### 20. `SITE_PLATFORM_DOMAIN` *(optional)*

| | |
|---|---|
| Where it comes from | A domain/subdomain you control |
| Already available? | Unknown |
| Must be generated? | No |
| Do you already own it? | Unknown — depends on whether you've registered a domain for custom-domain publishing |
| Example format | `sites.yourdomain.com` |
| Action needed | Only needed for the custom-domain publishing feature. May be left blank at deploy time. |

---

## `ordervora-web` Environment Variables

Added in Production Phase 2C, when `render.yaml` was extended to deploy `apps/web` on Render instead of Vercel (`docs/reports/RENDER_BLUEPRINT_FINAL.md`). Full detail in that report; summarized here for workbook completeness.

### 21. `NODE_ENV` (web)

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: production` |
| Must be generated? | No |
| Action needed | None — automatic |

### 22. `PORT` (web)

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: "3000"` |
| Must be generated? | No |
| Action needed | None — automatic |

### 23. `API_URL_SCHEME`

| | |
|---|---|
| Where it comes from | Literal, fixed by `render.yaml` |
| Already available? | Yes — `value: http` (Render's private network between services is plain HTTP, not HTTPS) |
| Must be generated? | No |
| Action needed | None — automatic |

### 24. `API_HOST`

| | |
|---|---|
| Where it comes from | Render itself, via `fromService` — resolves to `ordervora-api`'s internal `host:port` on Render's private network |
| Already available? | Yes — resolved automatically on every deploy/sync |
| Must be generated? | No |
| Do you already own it? | N/A — not a credential, computed by Render |
| Example format | `ordervora-api:4000` (internal address — never a public URL) |
| Action needed | None — automatic. This is what makes web↔API internal communication automatic in both directions of build/runtime (see `apps/web/Dockerfile`'s use of it, detailed in `docs/reports/RENDER_BLUEPRINT_FINAL.md`) |

### 25. `NEXT_PUBLIC_SITE_URL`

| | |
|---|---|
| Where it comes from | You choose it — your production domain |
| Already available? | No — depends on whether you have a custom domain |
| Must be generated? | No |
| Do you already own it? | Unknown — depends on domain ownership |
| Example format | `https://yourdomain.com` (or leave as `ordervora-web`'s own Render-assigned URL if no custom domain exists yet) |
| Action needed | Paste your production domain, or a placeholder to fix later, when Render prompts |

---

## Deployment Order

This is the exact order Render's Blueprint UI will prompt for values (matches `render.yaml`'s declaration order — `ordervora-api` first, then `ordervora-web`, since that's the service order in `render.yaml`). Variables with no prompt (automatic) are listed for completeness but need no action.

### `ordervora-api`

| Order | Variable | Action |
|---|---|---|
| — | `NODE_ENV` | No action — automatic |
| — | `PORT` | No action — automatic |
| **1st** | `DATABASE_URL` | Paste your completed Supabase Session Pooler string |
| **2nd** | `FRONTEND_URL` | Paste a temporary placeholder — replace with `ordervora-web`'s real URL after both services exist |
| — | `JWT_ACCESS_SECRET` | No action — Render auto-generates |
| — | `JWT_REFRESH_SECRET` | No action — Render auto-generates |
| — | `JWT_ACCESS_TTL` | No action — automatic |
| — | `JWT_REFRESH_TTL` | No action — automatic |
| **3rd** | `COMMERCE_ENCRYPTION_KEY` | Paste the generated value (delivered privately — see below) |
| **4th** | `ADMIN_EMAIL` | Paste your chosen admin email |
| **5th** | `ADMIN_PASSWORD` | Paste your chosen strong admin password |
| **6th** | `ADMIN_NAME` | Paste your chosen admin display name |
| **7th** | `SMTP_HOST` | Paste your SMTP provider's host |
| **8th** | `SMTP_PORT` | Paste your SMTP provider's port |
| **9th** | `SMTP_USER` | Paste your SMTP provider's username |
| **10th** | `SMTP_PASSWORD` | Paste your SMTP provider's password/API key |
| **11th** | `SMTP_FROM_ADDRESS` | Paste your chosen sender address |
| **12th** | `OPENAI_API_KEY` | Paste your OpenAI key |
| **13th** | `GOOGLE_MAPS_API_KEY` | Paste if using Places import, otherwise leave blank |
| **14th** | `SITE_PLATFORM_DOMAIN` | Paste if using custom domains, otherwise leave blank |

### `ordervora-web`

| Order | Variable | Action |
|---|---|---|
| — | `NODE_ENV` | No action — automatic |
| — | `PORT` | No action — automatic |
| — | `API_URL_SCHEME` | No action — automatic |
| — | `API_HOST` | No action — automatic, resolved via `fromService` |
| **15th** | `NEXT_PUBLIC_SITE_URL` | Paste your production domain, or a placeholder to fix later |

### After both services exist

| Order | Variable | Action |
|---|---|---|
| **16th (final step)** | `FRONTEND_URL` (back on `ordervora-api`) | Return and paste `ordervora-web`'s real URL, replacing the step-2 placeholder |

**You need to obtain, before starting:** a live Supabase project + its Session Pooler `DATABASE_URL` (step 1), an SMTP provider account (steps 7-11), and an OpenAI API key (step 12) — none of these were confirmed to already exist in this engagement. Everything else is either automatic, generated for you, or your own free choice.

---

## About the Generated Secret

`COMMERCE_ENCRYPTION_KEY` was generated for this deployment using `openssl rand -hex 32` and validated to be exactly 64 lowercase hex characters, matching `apps/api/src/config/env.ts`'s startup validation requirement.

**The real value is not in this file and was never committed to this repository.** `abukeeth/core` is a public GitHub repository — committing a real production secret here would permanently expose it (git history persists even after a later deletion, and public repos are routinely scraped). This matches the rule `render.yaml` and `docs/runbooks/database-setup.md` already state for every other secret in this project.

The generated value was delivered to you directly as a private file in this session, separate from the git repository. Copy it from there into Render's `COMMERCE_ENCRYPTION_KEY` prompt, then store it in a password manager. If you believe it was ever exposed, rotate it using the procedure in `docs/runbooks/secret-rotation.md` before relying on it in production.
